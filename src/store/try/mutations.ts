import { MutationTree } from 'vuex';
import { TryDataStateInterface } from './state';

const mutation: MutationTree<TryDataStateInterface> = {
  someMutation (/* state: TryDataStateInterface */) {
    // your code
  }
};

export default mutation;
